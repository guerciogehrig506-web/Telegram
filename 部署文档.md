# Trio 免费部署文档

> 目标：将 Trio 后端从你的电脑迁移到云端，**零费用、零信用卡**，7×24 运行。

---

## 架构总览

```
部署前（现在）：
  Android APP ──► localhost:3000（你的电脑，关机就停）

部署后：
  Android APP ──► Render（云端，不关机）
                    │
                    ├──► Turso（云端 SQLite，数据永久保存）
                    │
                    └──► Firebase（保持不变：FCM 推送 + Firestore 只读）

  UptimeRobot ──► 每 14 分钟 ping Render，防休眠
```

---

## 一、注册账号（10 分钟）

按顺序注册以下三个账号，都不需要信用卡：

| 序号 | 平台 | 地址 | 用途 | 用什么注册 |
|------|------|------|------|-----------|
| 1 | **Turso** | [turso.tech](https://turso.tech) | 云端 SQLite 数据库 | GitHub 账号 |
| 2 | **Render** | [render.com](https://render.com) | 跑 NestJS 后端 | GitHub 账号 |
| 3 | **UptimeRobot** | [uptimerobot.com](https://uptimerobot.com) | 防止 Render 休眠 | 邮箱 |

---

## 二、Turso — 创建云端数据库（5 分钟）

### 2.1 安装 Turso CLI

在电脑上打开终端（PowerShell），执行：

```powershell
# Windows (PowerShell)
winget install tursodatabase.turso
```

如果 winget 不可用，用 npm 安装：
```powershell
npm install -g turso
```

### 2.2 登录并创建数据库

```powershell
turso auth login
# → 会打开浏览器，用 GitHub 授权

turso db create trio-db
# → 创建一个名为 trio-db 的数据库

turso db show trio-db
# → 记下显示的 URL，类似：libsql://trio-db-你的名字.turso.io

turso db tokens create trio-db
# → 生成访问令牌，记下这个令牌（只显示一次！）
```

### 2.3 获取的信息汇总

| 变量 | 值示例 | 说明 |
|------|--------|------|
| `DATABASE_URL` | `libsql://trio-db-xxxx.turso.io` | Turso 数据库地址 |
| `TURSO_AUTH_TOKEN` | `eyJ...很长一串...` | 访问令牌 |

> ⚠️ 令牌只显示一次，务必保存！丢了就再运行 `turso db tokens create trio-db` 生成新的。

---

## 三、修改项目代码（5 分钟）

### 3.1 安装新依赖

```powershell
cd backend
npm install @prisma/adapter-libsql @libsql/client
```

### 3.2 修改 `prisma/schema.prisma`

在文件顶部，把：

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
```

改为：

```prisma
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider     = "sqlite"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}
```

### 3.3 修改 `src/prisma/prisma.service.ts`

把：

```typescript
import { Injectable, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit {
  async onModuleInit() {
    await this.$connect();
  }
}
```

改为：

```typescript
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';
import { PrismaLibSQL } from '@prisma/adapter-libsql';
import { createClient } from '@libsql/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  constructor() {
    const libsql = createClient({
      url: process.env.DATABASE_URL!,
      authToken: process.env.TURSO_AUTH_TOKEN!,
    });
    super({ adapter: new PrismaLibSQL(libsql) });
  }

  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

### 3.4 修改 `.env`

```env
JWT_SECRET=trio-secret-key-change-in-production
JWT_EXPIRES_IN=24h
PORT=3000
DATABASE_URL=libsql://trio-db-xxxx.turso.io
TURSO_AUTH_TOKEN=eyJ...你的令牌
SESSION_SECRET=trio-admin-session-production
```

> 把 `DATABASE_URL` 和 `TURSO_AUTH_TOKEN` 换成你在 2.3 步骤中获取的值。

### 3.5 重新生成 Prisma Client + 推送到 Turso

```powershell
npx prisma generate
npx prisma db push
```

看到 `Your database is now in sync with your schema.` 就成功了。

### 3.6 本地验证一下

```powershell
npm run start:dev
```

确认后端能正常启动，没有报错。然后 `Ctrl+C` 停掉。

---

## 四、Render — 部署后端（10 分钟）

### 4.1 先把代码推送到 GitHub

如果你的项目还没有 GitHub 仓库：

```powershell
cd c:\Users\Administrator\Desktop\Telegram
git init
git add backend/
git commit -m "backend with turso"
```

然后在 GitHub 上创建仓库，push 上去。

> 如果只用 Render 跑后端，可以只上传 `backend/` 目录。但整个项目一起 push 更方便。

### 4.2 Render 设置

1. 打开 [render.com](https://render.com)，用 GitHub 登录
2. 点右上角 **New +** → **Web Service**
3. 连接你的 GitHub 仓库
4. 填写以下配置：

| 配置项 | 值 |
|--------|-----|
| **Name** | `trio-backend`（任意） |
| **Region** | `Singapore`（离中国最近） |
| **Branch** | `main` |
| **Root Directory** | `backend` |
| **Runtime** | `Node` |
| **Build Command** | `npm install && npx prisma generate && npm run build` |
| **Start Command** | `npm run start:prod` |
| **Instance Type** | **Free** |

### 4.3 设置环境变量

在 Render 页面找到 **Environment** 部分，添加以下变量：

| Key | Value |
|-----|-------|
| `JWT_SECRET` | `trio-secret-key-change-in-production` |
| `JWT_EXPIRES_IN` | `24h` |
| `PORT` | `3000` |
| `DATABASE_URL` | `libsql://trio-db-xxxx.turso.io` |
| `TURSO_AUTH_TOKEN` | `eyJ...` |
| `SESSION_SECRET` | `trio-admin-session-production` |

> ⚠️ `DATABASE_URL` 和 `TURSO_AUTH_TOKEN` 必须和 Turso 步骤中的值一致。

### 4.4 部署

点 **Create Web Service**，Render 会自动：
1. Clone 代码
2. 执行 `npm install`
3. 执行 `prisma generate`
4. 编译 NestJS
5. 启动服务

等待 3-5 分钟，看到 `Trio Backend running on http://0.0.0.0:3000` 即成功。

### 4.5 获取 Render 地址

部署成功后，页面上方会显示你的 URL，类似：

```
https://trio-backend-xxxx.onrender.com
```

记下这个地址，这是后端的新地址。

---

## 五、UptimeRobot — 防止休眠（3 分钟）

Render 免费版在 15 分钟无人访问后会休眠。UptimeRobot 每 14 分钟 ping 一次，让它保持唤醒。

1. 打开 [uptimerobot.com](https://uptimerobot.com)，用邮箱注册
2. 点 **+ Create New Monitor**
3. 选择 **Monitor Type: HTTP(s)**
4. 填写：

| 配置项 | 值 |
|--------|-----|
| **URL** | `https://trio-backend-xxxx.onrender.com` |
| **Monitoring Interval** | `14 minutes` |
| **Monitor Name** | `Trio Backend` |

5. 点 **Create Monitor**

完成。从此 Render 不会再休眠。

---

## 六、Android 端修改（2 分钟）

修改 `android/app/src/main/java/com/trio/app/data/api/ApiClient.kt`：

```kotlin
private const val BASE_URL = "https://trio-backend-xxxx.onrender.com/"
```

> 把地址换成你在 4.5 步骤中获取的 Render 地址。**注意末尾有 `/`，必须是 HTTPS。**

然后重新编译 APK：

```powershell
cd android
$env:JAVA_HOME = "C:\Program Files\Java\jdk-17"
.\gradlew assembleDebug
```

APK 在 `android/app/build/outputs/apk/debug/app-debug.apk`。

---

## 七、验证清单

| 验证项 | 怎么测 |
|--------|--------|
| ✅ 后端启动 | 浏览器打开 Render 地址，应返回 JSON 或 404（说明服务在跑） |
| ✅ 注册登录 | 用 APP 注册新用户，登录成功 |
| ✅ 发消息 | 发一条消息，对方能收到 |
| ✅ 数据库持久 | 在 Render 上手动重启服务 → 重新登录 → 之前的消息还在 |
| ✅ 休眠防护 | 等 30 分钟不用 APP → 打开 APP 应该秒响应（不会等 30 秒） |

---

## 八、注意事项

| 问题 | 说明 |
|------|------|
| **Admin 后台登录** | Admin 使用 EJS 模板 + Session 登录，Render 重启后 Session 丢失，需要重新登录一次（不影响 APP 用户）。 |
| **FCM 推送** | 推送功能仍然需要 `google-services.json`，与部署无关，后续单独配置。 |
| **上传文件** | `backend/uploads/` 在 Render 上是临时的，重启丢失。图片上传走 Firebase Storage 不受影响，Multer 上传的文件（头像等）如果很重要需要额外处理。 |
| **免费额度** | Render 每月 750 小时（刚好一个月），Turso 9GB + 10亿行读取，10 人团队绰绰有余。 |
| **HTTPS** | Render 自动提供 HTTPS 证书，无需配置。 |

---

## 九、回滚方案

如果部署出问题，随时可以回到本地模式：

1. 把 `ApiClient.kt` 的 `BASE_URL` 改回 `http://192.168.0.111:3000/`
2. 把 `.env` 的 `DATABASE_URL` 改回 `file:./dev.db`
3. 把 `prisma.service.ts` 改回原来版本
4. 运行 `npx prisma generate && npm run start:dev`

---

## 十、帮你统计一下

| 步骤 | 耗时 | 难度 |
|------|------|------|
| 注册三个账号 | 10 分钟 | ⭐ 点点鼠标 |
| Turso 建库 | 5 分钟 | ⭐⭐ 几条命令 |
| 改代码（3 个文件） | 5 分钟 | ⭐⭐ 复制粘贴 |
| Render 部署 | 10 分钟 | ⭐ 填表即可 |
| UptimeRobot | 3 分钟 | ⭐ 填表即可 |
| Android 改地址 + 编译 | 5 分钟 | ⭐ 改一个单词 |
| **总计** | **~40 分钟** | |