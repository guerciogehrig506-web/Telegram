# Trio 开发计划与进度

> 最后更新: 2026-05-10

## 整体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 1A — 后端补齐 | ✅ 完成 | NestJS + Prisma + SQLite, JWT 全局鉴权, WebSocket, admin 面板 |
| Phase 1B — 前端回接 | ✅ 完成 | Retrofit + REST API, 注册/登录/聊天/通讯录/朋友圈全闭环 |
| Phase 2A — 单人聊天完整体验 | ✅ 完成 | 图片收发 + 相册拍照 + 搜索 + 骨架屏 + 资料编辑 + 打字指示器 |
| Phase 2B — 社交扩展 | ✅ 完成 | 群聊 CRUD + 创建/聊天页面/导航 |
| Phase 3 — 动效打磨与适配 | 🔜 进行中 | 导航过渡/消息动画/列表动画/底部栏动效 已完成；剩余：下拉刷新/触觉/平板/暗色 |
| Phase 4 — 性能与内存优化 | ⏳ 待开始 | LazyColumn 治理 / 图片内存 / 启动优化 / LeakCanary |
| Phase 5 — 工程化与发布 | ⏳ 待开始 | 测试 / 混淆 / Play Store 上架 |

***

## 最新变更 (2026-05-10)

### 架构确认
- Android 客户端已完全切换到后端 REST API（不再直接使用 Firebase Auth/Firestore SDK）
- 后端 SQLite 为主数据库，Firestore 仅用于 Admin 看板数据展示（单向同步）
- Firebase 在 Android 端仅保留：FirebaseApp 初始化（供 Storage 图片上传使用）

### 代码清理
- 删除未引用的冗余文件：`FirestoreModels.kt`, `ChatRepository.kt`, `UserRepository.kt`, `MomentsRepository.kt`, `SocketManager.kt`, `TasksPlaceholderScreen.kt`, `NgrokInterceptor.kt`
- 精简 `FirebaseAuthManager.kt`：移除未使用的 `register()/login()/checkSession()` 方法及 `FirebaseResult/FirestoreState` 类型
- 移除 `ApiClient` 中的 ngrok 拦截器
- `TrioApp` logout 路径增加 `SessionManager.logout()` 调用
- 修复 SplashScreen 导航时序问题（navigate 在 NavHost 创建前调用导致闪退）

### 待修复的已知问题
1. **FirebaseAuthManager.getUserId() 返回 null**: 多个 ViewModel/Screen 使用此方法获取当前用户 ID，但 APP 已切到 REST API 认证，Firebase Auth 未登录，应替换为 `SessionManager.currentUser.value?.id`
   - 涉及文件: ChatScreen.kt, MomentsScreen.kt, ProfileEditScreen.kt, SettingsScreen.kt, CreateGroupViewModel.kt
2. **SplashScreen 闪退**: 已修复（navigate 时序问题）
3. **Admin 看板需 Firebase Auth 用户**: 需在 Firebase Console 手动创建管理员账号

***

## Phase 3 — 动效打磨与适配 🔜 当前阶段

### 已完成

| # | 子任务 | 状态 | 备注 |
| - | ------ | -- | -- |
| 1 | 导航页面过渡动画 | ✅ | NavHost slideInHorizontally + fadeIn/Out (350ms) |
| 2 | 消息气泡入场动画 | ✅ | ChatScreen/GroupChatScreen 消息项 AnimatedVisibility |
| 3 | 列表项交互动画 | ✅ | ChatListScreen/ContactsScreen/MomentsScreen AnimatedListItem |
| 4 | 底部导航栏入场动效 | ✅ | LiquidGlassBottomBar AnimatedVisibility + scale/fade |
| 5 | 按压缩放反馈 | ✅ | pressScale Modifier 可复用 |
| 6 | 窗口自适应 | ✅ | WindowAdaptive 工具类（compact/medium/expanded） |

### 待完成

| # | 子任务 | 状态 | 备注 |
| - | ------ | -- | -- |
| 1 | 下拉刷新动画 | ⏳ | 自定义 PullRefreshIndicator，匹配 Telegram 风格微动效 |
| 2 | 触觉反馈 | ⏳ | 发送消息、长按菜单、点赞操作触发 HapticFeedback |
| 3 | 侧滑手势 | ⏳ | 聊天列表项侧滑显示删除/归档操作（SwipeToDismissBox） |
| 4 | 暗色模式完善 | ⏳ | 补全所有页面暗色适配，跟随系统 + 手动切换入口 |
| 5 | 平板适配 | ⏳ | 大屏双栏布局（聊天列表 + 聊天详情并排），横屏优化 |

***

## Phase 4 — 性能与内存优化

| # | 子任务 | 状态 | 备注 |
| - | ------ | -- | -- |
| 1 | LazyColumn 性能治理 | ⏳ | 为所有消息列表、通讯录添加 stable keys；排查不必要的重组 |
| 2 | 图片内存优化 | ⏳ | 检查 Coil 缓存命中率，大图下采样策略调整，Bitmap 及时回收 |
| 3 | 启动耗时优化 | ⏳ | 移除启动阶段非必要初始化，延迟加载部分模块 |
| 4 | 内存泄漏排查 | ⏳ | 接入 LeakCanary，重点排查 ViewModel、图片加载链路 |
| 5 | 帧率监控 | ⏳ | Debug 模式下开启 Compose 布局检查器，定位卡顿 Composable |

***

## Phase 5 — 工程化与发布

| # | 子任务 | 状态 | 备注 |
| - | ------ | -- | -- |
| 1 | 代码重构与清理 | ✅ | 2026-05-10 已完成冗余代码清理 |
| 2 | 单元测试补齐 | ⏳ | ViewModel 测试（Auth、Chat、Contacts），Repository 测试 |
| 3 | UI 测试 | ⏳ | Compose UI Test 覆盖登录、发消息、通讯录浏览等核心流程 |
| 4 | Release 签名与混淆 | ⏳ | 生成 Release keystore，配置 ProGuard/R8 规则 |
| 5 | Play Store 上架准备 | ⏳ | 应用详情页素材、隐私政策、内容分级问卷 |